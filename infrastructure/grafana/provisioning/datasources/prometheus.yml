# Auto-provisions the Prometheus datasource on Grafana startup — see docker-compose.yml's `grafana` service.
apiVersion: 1

datasources:
  - name: Prometheus
    type: prometheus
    access: proxy
    url: http://prometheus:9090
    isDefault: true
