-r requirements.txt
pytest==9.1.1
pytest-asyncio==1.4.0
# httpx is a runtime dependency now (requirements.txt) — the API's own test client (ASGITransport) uses it too.
aiosqlite==0.22.1
fakeredis==2.38.0
ruff==0.16.8
