"""Feature modules. Each owns its models, schemas, service (business logic) and router (HTTP).

Planned (see docs/architecture.md): problems, submissions, judge, ai, contests, leaderboard,
discussions, notifications, analytics. Modules are added phase by phase, never stubbed early.
"""
